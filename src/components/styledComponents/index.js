// External Imports
import styled, { css } from 'styled-components'

export const Container = styled.div`
  margin: 0 auto;
  width: 100%;
  max-width: 1280px;
  padding: 20px;
  background: var(--primary-blue);
`

export const HorizontalNavbar = styled.nav`
  width: 100%;
  background: var(--primary-color);
`
